version: 1.0
runtime: go1
build:
  commands:
     build:
       - go mod download
       - go build -o bin/application ./cmd/server
     run:
       runtime-version: 1.18.7
       command: ./bin/application
       network:
         port: 5000
